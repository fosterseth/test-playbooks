- name: Create an organization
  {{ collection_id }}.organization:
    name: {{ organization_name | default("Test Org name") }}
    default_environment: {{ default_environment }}
    description: {{ description | default("default description") }}
  register: org
